# LaTeX Skeletons

This folder is for project-owned LaTeX skeleton files.

The generators can already write `.tex` files directly. These files exist to document local defaults and to support future class-specific refinement.
